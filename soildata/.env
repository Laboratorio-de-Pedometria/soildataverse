# SOILDATA Production Configuration
# =================================

# Dataverse Docker Image
VERSION=6.2.allclouds
DOCKER_HUB=coronawhy/dataverse

# Domain Configuration
# Replace with your actual domain
traefikhost=soildata.example.com
hostname=soildata.example.com

# SSL/Email Configuration for Let's Encrypt
# IMPORTANT: Replace with your actual email
useremail=admin@soildata.example.com
ADMIN_EMAIL=admin@soildata.example.com

# Dataverse Database Settings
DATAVERSE_DB_HOST=postgres
DATAVERSE_DB_USER=soildata_user
DATAVERSE_DB_PASSWORD=soildata_secure_password_2024
DATAVERSE_DB_NAME=soildata

# PostgreSQL Settings  
POSTGRES_USER=soildata_user
POSTGRES_PASSWORD=soildata_secure_password_2024
POSTGRES_SERVER=postgres
POSTGRES_DATABASE=soildata
POSTGRES_DB=soildata
POSTGRES_PORT=5432

# Solr Configuration
SOLR_SERVICE_HOST=solr:8983
SOLR_SERVICE_PORT=8983
DATAVERSE_URL=soildata.example.com
DATAVERSE_SERVICE_HOST=soildata.example.com

# MinIO Storage Configuration
MINIO_ROOT_USER=soildata_admin
MINIO_ROOT_PASSWORD=soildata_minio_password_2024

# Paths and Configuration
CONFIGURATION_PATH=.
LOCAL_STORAGE=.
DOCROOT=.
INIT_SCRIPTS_FOLDER=/opt/payara/init.d

# Language Configuration
MAINLANG=en

# DOI Configuration (DataCite Production)
# IMPORTANT: Configure with your actual DataCite credentials
doi_authority=10.XXXXX
doi_provider=datacite
doi_username=YOUR_DATACITE_USERNAME
doi_password=YOUR_DATACITE_PASSWORD
dataciterestapiurlstring=https\\:\/\/api.datacite.org
baseurlstring=https\:\/\/mds.datacite.org

# Email/SMTP Configuration
# IMPORTANT: Configure with your actual SMTP settings
system_email=noreply@soildata.example.com
mailhost=smtp.example.com
mailuser=smtp_user
no_reply_email=noreply@soildata.example.com
smtp_password=smtp_password
smtp_port=587
socket_port=25

# MinIO Bucket Configuration for Production
bucketname_1=soildata-files
minio_label_1=SOILDATA Files Storage
minio_bucket_1=soildata-files
minio_profile_1=soildata-profile

bucketname_2=soildata-backup
minio_label_2=SOILDATA Backup Storage  
minio_bucket_2=soildata-backup
minio_profile_2=soildata-backup-profile

# AWS S3 Configuration (if using AWS instead of MinIO)
# Uncomment and configure if you prefer AWS S3
#aws_bucket_name=soildata-aws-bucket
#aws_s3_profile=soildata-aws-profile
#aws_endpoint_url=https://s3.amazonaws.com

# Webhook Configuration
WEBHOOK=/opt/payara/triggers/external-services.py

# Custom Metadata Support (uncomment if needed)
#CESSDA=True
#CLARIN=True

# Federated Authentication
# Uncomment and configure if using Shibboleth/SAML
#federated_json_file=federated_json_file

# Environment Variables
PGPASSWORD=soildata_secure_password_2024
BUNDLEPROPERTIES=Bundle.properties
LANG=en
CVM_SERVER_NAME=SOILDATA
TWORAVENS_LOCATION=NOT INSTALLED
RSERVE_HOST=localhost
RSERVE_PORT=6311
RSERVE_USER=rserve
RSERVE_PASSWORD=rserve